import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#0098CD",
    },
    background: {
      default: "#fff",
    },
  },
});

export default theme;
